import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#512da8"
        },
        secondary: {
            main: "#1de9b6"
        },
        text: {
            primary: "#000",
            secondary: "#000"
        }
    }
});

export default theme;
